<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CurrencyTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CurrencyTask" parent="Base.Theme.CurrencyTask" />

    <style name="TextInputOutlineLayoutStyle" parent="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="boxStrokeWidth">1dp</item>
        <item name="boxStrokeColor">@color/currency_green</item>
        <item name="colorError">@color/color_red</item>
        <item name="errorTextColor">@color/color_red</item>
        <item name="shapeAppearance">@style/roundShapeStyle</item>
    </style>

    <style name="roundShapeStyle">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <style name="MaterialButtonOutlineStyle" parent="@style/Widget.MaterialComponents.Button.TextButton">
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="backgroundTint">@color/currency_green</item>
        <item name="android:textStyle">bold</item>
        <item name="strokeColor">@color/white</item>
        <item name="strokeWidth">1dp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:paddingStart">8dp</item>
        <item name="shapeAppearanceOverlay">@style/roundShapeStyle</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>